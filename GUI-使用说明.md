# Clash-SpeedTest 图形界面使用说明

## 启动

双击 `Clash-SpeedTest-GUI.exe`。以下三个程序需要放在同一文件夹：

- `Clash-SpeedTest-GUI.exe`
- `subscription-parser.exe`
- `speedtest-runner.exe`

`clash-speedtest.exe` 是可选的上游命令行程序，GUI 不依赖它。

## 基本使用

1. 在“配置/订阅/节点”中粘贴 HTTPS 订阅地址、单节点链接或选择本地 YAML 配置。多个输入必须每行一个，不使用逗号分隔；URL、Windows 文件名和节点参数中的逗号会原样保留。
2. 选择测速方案。第一次使用建议选择“均衡（推荐）”。
3. 根据需要修改延迟上限、下载速度下限和输出文件；需要在测速前按名称缩小节点范围时，可在“高级设置”中填写节点正则过滤条件。
4. 点击“开始测速”。
5. GUI 会先列出全部待测节点，按“延迟并发（旧称节点并发）”进行低流量 HTTP 初筛；每个节点完成后立即显示“失败”或“等待下载”。均衡/深度模式随后逐节点执行下载，当前节点显示“下载中”，完成后立即更新最终结果。
6. 只有绿色“有效”的节点会写入输出 YAML；无法测出延迟、HTTP 探测全部失败、吞吐传输未完成或不满足筛选条件的节点不会导出。
7. 完成后可以点击“打开结果位置”或“复制结果路径”。

任务开始后，输入、输出、阈值、测速方案、重命名、Gist 和高级参数会锁定，避免界面显示值与实际运行参数不同；列表筛选和复制等只读操作仍可使用。点击“停止”会取消订阅解析、测速、出口地区查询、结果后处理以及正在等待的 Gist 请求。关闭窗口时，程序会先停止任务并清理临时文件，再真正退出。

测速状态栏会持续显示 `已探测 x/总数`、`等待下载 y`、`正在下载：节点名称` 以及有效、失败、等待数量。子进程退出、协议错误或取消时，所有仍在等待的节点会立即改为失败并显示原因，不会永久停在“等待”。

快捷键：`F5` 开始测速，`F6` 查询尚未成功识别地区的有效节点，任务运行时按 `Esc` 停止。它们在焦点位于多行输入框或结果表格时仍然生效；如果组合框下拉列表已展开，`Esc` 只关闭下拉列表，不会停止任务。

输出文件不能与任何本地输入配置、嵌套的本地 `type: file` Proxy Provider、GUI、订阅解析器或测速器程序相同；程序会在测速启动前和最终原子提交前分别检查，检测到路径冲突时直接阻止任务，不进入普通覆盖提示。

远程订阅和直接粘贴内容按不可信来源处理：其中的 HTTP Proxy Provider 每次连接都会重新解析 DNS，并根据程序内注明日期的 IANA 特殊用途地址注册表快照做最长前缀资格判断；注册表标记为非全局、空值或 N/A 的地址会被阻止，更具体的全局例外仍可使用。未列入特殊用途注册表的 global-unicast 地址允许尝试连接，因此这里是防止访问已知本机、私网和特殊用途网段的安全边界，不保证目标在所有网络中实际公开可路由；注册表更新后需同步更新程序。如确实需要读取本机或局域网 HTTP Provider，请把配置保存为本地 YAML 后通过“选择文件”明确打开。

## 节点列表操作

- 单击选择一个节点，按住 `Ctrl` 单击可多选，按住 `Shift` 单击可连续选择。
- 节点列表获得焦点时，`Ctrl+A` 全选，`Ctrl+C` 复制所选节点 URL；多个节点按当前显示顺序每行一个。
- 右键节点可以复制节点 URL、Clash Meta 配置或节点名称。右键已选节点会保留整个多选集合，右键未选节点会改为只选择该节点。
- 主列表固定为七列：`序号、节点名称、类型、HTTP 延迟、下载速度、出口地区、状态`。快速模式不执行下载，对应单元格显示“未测试”。
- 点击节点名称、类型、HTTP 延迟、下载速度、出口地区或状态列头可切换升序和降序。数值列的“未测试”和无结果始终排在有效数值之后；测速完成后默认按“有效节点优先、HTTP 延迟从低到高”排列。序号会按当前可见顺序自动重排。
- “列表筛选”可组合选择状态、延迟、协议和出口国家代码。四类条件按“同时满足”生效，只隐藏不匹配的列表行，不会改写输出 YAML。地区下拉框初始只有“全部”且不可选；点击“查询有效节点出口地区”后，它只列出本轮查询成功、列表中真实存在的出口国家，不受固定国家清单限制。未查询节点不会根据名称猜测地区。点击“重置”恢复全部显示。
- 右键选择“重命名节点”或按 `F2` 可以修改已导出节点的名称；未导出节点不能重命名。
- 可多选已导出的有效节点，右键选择“从结果中删除节点”或按 `Delete` 删除。程序会先确认，再原子更新输出 YAML；失败时保留原文件。
- 名称修改和删除都只作用于本地筛选结果，不会修改本地输入文件或远程订阅。再次测速时，订阅中的原节点可能重新出现。
- 合并多个输入源时，同名但配置不同的节点会全部保留；顶层节点与 `[Provider] 节点名` 合成名称碰撞、Provider override 产生同名时也不会覆盖。程序在 Provider filter/override 完成后统一唯一命名，第一个使用原名，后续依次自动编号为 `原名 [2]`、`原名 [3]`，并避让用户真实存在的编号名称。
- 如果已启用 Gist，本地保存成功后会自动同步程序专用 Gist；Gist 更新失败不会撤销已经成功保存的本地结果。

复制 Clash Meta 时直接使用测速器内部保存的完整节点配置，不会先转成 URL 再解析，因此 WebSocket、gRPC、Reality、SNI、ALPN 等嵌套参数仍会保留。复制分享 URL 时，每条生成结果都会立即交给生产使用的 Mihomo 转换器重新导入；只有恰好得到一个节点，并且协议、地址、端口、认证、TLS、传输、插件及协议专用连接参数经过缺省值和类型规范化后全部等价，才会标记为可复制。当前 SS `obfs` 插件的 `mode`、`host` 可以无损回环；Trojan WebSocket 的 Host/early-data、SSR IPv6、SSR `udp-over-tcp` 等生产导入器不能恢复的组合，以及额外 WebSocket header、Reality 高级项、路由依赖、端口跳跃或协议调优参数，会明确拒绝。一批选择中任一节点失败时不会调用剪贴板提交逻辑，原内容保持不变，也不会只复制合法节点组成的半份结果。

## 出口地区查询

- 测速完成后点击“查询有效节点出口地区”，只查询本轮已导出的有效节点，并跳过已有成功结果；右键“查询/刷新选中节点出口地区”可以强制刷新所选有效节点。
- 查询状态依次显示为 `—`、`未查询`、`查询中`、地区名称、`查询失败` 或 `不查询`。国家和城市相同时只显示一次，城市缺失时只显示国家，长内容可悬停查看完整文字；查询失败时悬停可查看超时或服务错误原因。
- 每个请求都通过对应节点代理访问。服务按 IPWHOIS.io、FreeIPAPI、IP.SB 的顺序逐一回退，不会同时请求多家；单服务超时 4 秒，单节点总预算 12 秒，同时最多查询 8 个节点。
- 遇到 403/429 时，本批次暂停对应服务；连续 3 次网络错误或 5xx 后，本批次熔断该服务。地区结果采用整批提交：只有查询器正常退出且地区协议 v2 的版本、声明数量、稳定 ID 和结果完整性全部通过后才更新界面。点击同一个“停止”按钮取消，或遇到重复、未知、缺失、畸形事件时，本批结果都不应用；刷新前已有的地区信息会完整恢复。
- 出口地区是 IP 数据库的近似定位。程序不显示、记录或保存出口 IP、ASN、运营商；地区结果不写入 YAML、不上传 Gist、不跨测速保存，重新测速会清空。

首次默认值：

- 延迟上限：`800ms`
- 下载速度下限：`3MB/s`
- 输出文件：`filtered.yaml`
- 重命名：关闭
- Gist 上传：关闭

这些设置都可以修改，程序会记住上次保存或使用的值。

对于 HTTPS 订阅地址，GUI 会自动添加 Mihomo 格式参数：

- 原地址没有查询参数时添加 `?flag=meta`
- 原地址已有 `?token=...` 等参数时添加 `&flag=meta`
- 原地址已有其他 `flag` 值或重复 `flag` 时，替换并去重为唯一的 `flag=meta`
- 误写成 `...&flag=meta` 但前面没有 `?` 时会自动修正

节点链接可直接粘贴，支持 `ss://`、`ssr://`、`vmess://`、`vless://`、`trojan://`、`hysteria://`、`hysteria2://`、`hy2://`、`tuic://`、`anytls://`、`socks://`、`socks5://` 和 `socks5h://`。多行输入会合并为一次本地解析，1000 个节点不会启动 1000 次解析进程。由于 `http://`、`https://` 无法可靠区分“订阅地址”和“HTTP 代理节点”，HTTP 代理单节点仍需保存为文本文件后通过“选择文件”导入。

远程订阅正文上限为 `32MiB`。恰好达到上限可以解析；超过 1 字节也会明确失败，不会截断后生成缺少尾部节点的配置。错误提示不会回显订阅正文、节点凭据或完整的带 Token URL。

本地 Clash/Mihomo YAML 支持 `type: file` 的 Proxy Provider，相对 `path` 按原 YAML 所在目录解析。远程 YAML 即使已由 GUI 落地为临时文件，仍保留不可信远程来源属性，绝对路径、相对路径、`../` 路径穿越和 HTTP Provider 缓存 `path` 都不能用于读取本机文件。任一输入源或 Provider 读取、下载、解析失败时，本批任务整体失败并保留旧输出，不会静默跳过后提交半份节点。

HTTP Proxy Provider 支持自定义 `header`、字节级 `size-limit`、`proxies:`/`payload:` YAML 和 URI 列表，并且每个 Provider 只获取一次。任何 HTTPS 到 HTTP 的降级重定向都会在下一跳发出前失败；配置了自定义 Header、URL userinfo 或任意查询参数时只允许 scheme、hostname、有效端口均相同的严格同源重定向。无凭据 Provider 可以跨源跳转到 CDN，但新目标请求会先清除继承的 Header 和 `Referer`，每一跳仍使用受限拨号器。当前版本遇到 Provider 下载字段 `proxy`、`age-secret-key` 或非空节点字段 `dialer-proxy` 会在测速前明确拒绝；`dialer-proxy` 无论来自顶层节点、inline/file/HTTP Provider 节点还是 Provider override，都会使整批任务失败，不会被误报为普通网络不可达。完整支持该字段需要另行实现依赖闭包、循环、过滤、重命名和导出引用；`type` 与 `url`、`path`、`payload` 冲突时同样整批失败。

如果内核没有加载到任何节点，GUI 会明确报错，不再把空配置误报为测速成功。GUI 严格检查测速事件协议 v5；每个节点必须先且只能出现一次 `probe_completed`，下载模式只有通过初筛的节点才能随后出现一次 `download_started`。版本、表头、节点数量、稳定 ID、固定字段、重复、乱序、未知 ID、缺失事件或结果数量不一致时会报“内核输出协议错误”，并拒绝提交半截结果。协议中的 `tested` 表示下载已经启动，`complete` 表示计划传输全部完成；界面状态、排序和筛选使用内核给出的原始指标与权威有效状态，格式化文字只用于显示。

测速失败、没有节点通过筛选，或在本地结果提交前停止时，都不会覆盖已有输出文件。如果停止发生在本地结果已经原子提交之后，新本地文件会保留，但后续节点清单同步或 Gist 上传会停止；状态栏会区分这两种情况。大量节点的列表更新会按批次处理，筛选、排序和统计在每批结束后统一刷新。

## 测速方案

- **快速（仅延迟）**：只检查 HTTP 延迟和连通性，探测超时 `2s`、延迟并发 `8`，几乎不消耗测速流量。
- **均衡（推荐）**：探测超时 `3s`、下载超时 `8s`、每节点最多约 `20MB`、单节点 `4` 条下载连接。
- **深度（大流量纯下载）**：探测超时 `3s`、下载超时 `10s`、每节点最多约 `50MB`、单节点 `4` 条下载连接，并使用更严格的延迟、速度和 HTTP 探测失败率阈值。
- **自定义**：手动调整高级参数后自动切换到此状态。

流量按节点计算。例如 40 个节点使用均衡方案，理论上最多约使用 `800MB`。
程序会限制每条连接实际读取的最大字节数；服务器忽略 `Range` 时也不会继续读取完整文件。返回部分内容时还会严格校验 `Content-Range`。所有方案都是纯下载：带路径的测速地址直接使用 `Range`，不带路径的测速服务使用 `/__down`。

测速会先并发执行低流量 HTTP 探测，再让通过初筛的节点逐个执行下载。这样能避免待测节点彼此争抢本机可用带宽；单节点内部仍可使用多条连接，其他应用也仍可能影响测速结果。候选节点越多，总耗时越长，但结果通常更稳定。快速模式按节点完成探测的顺序立即返回最终结果，不等待整批完成。

每个节点先做 1 次不计入统计的预热，再做 5 次正式 GET 探测，样本之间间隔 100ms。直链使用 `Range: bytes=0-0`，无路径测速服务使用 `/__down?bytes=1`；延迟取成功样本的中位数。“HTTP 探测失败率”是 5 次正式 HTTP 请求中的失败比例，不是 ICMP/UDP 丢包率。

探测请求使用独立的“探测超时”，下载传输使用“下载超时”，两者互不影响。如果下载超时前没有传完计划字节数，GUI 会显示按实际字节数和实际耗时算出的部分速度，同时标记“传输未完成”。该节点不会按完整测速成功处理，也不会写入有效节点输出。

下载速度表示 `当前电脑网络 → 代理节点 → 当前测速服务器` 这条路径在本次测试时段的吞吐，不是节点在所有网站、所有地区和所有时间段的绝对带宽。

GUI 会在本机自动识别以下订阅格式：

- Clash/Mihomo YAML
- Base64 编码的通用协议订阅
- 明文协议链接列表
- `vless://`、`vmess://`、`trojan://`、`ss://`、`ssr://`
- `hysteria://`、`hysteria2://`、`hy2://`、`tuic://`、`anytls://`
- HTTP、HTTPS 和 SOCKS 代理链接

转换完全在本机进行，不会把订阅发送给在线转换网站。临时配置在测速结束或失败后自动清理。

## 节点重命名

勾选“按真实出口地区重命名节点”后，程序会先完成测速临时结果，再让每个有效节点通过自身代理查询出口地区，名称格式类似：

```text
🇭🇰 香港 HK-01
```

单个节点地区识别失败时保留原节点名称，不根据配置入口 `server` 或原名称猜测。整批地区协议异常时不应用任何自动重命名；用户在本地原子提交前停止时保留旧输出文件。查询成功得到的国家和城市会同时显示在当前列表中，本地提交完成后才会同步 Gist。

## GitHub Gist

1. 勾选“测速成功后自动创建或更新 GitHub Gist”。
2. 输入 GitHub 用户名。
3. 输入仅授予 `gist` 权限的 GitHub Token。
4. 测速和本地文件处理成功后，程序会按 GUI 输出文件名查找描述为 `Clash-SpeedTest GUI: 文件名` 的专用秘密（不公开列出）Gist。
5. 不同文件名会分别创建独立 Gist，获得不同的 Gist ID 和稳定 raw 订阅链接；同名文件再次同步时才覆盖原 Gist 内容并保持链接不变；改文件名等同于创建新的 Gist 链接。
6. 完成窗口显示当前文件的 `gist.githubusercontent.com/.../独立GistID/raw/文件名` 订阅链接。无需手动填写 Gist 地址或 ID。

程序会校验 Token 所属账号与填写的用户名是否一致，避免把结果传到错误账号。
测速后同步 Gist 时可以点击“停止”取消等待和后续请求；如果 GitHub 已经收到最终创建或更新请求，客户端取消无法撤销远端变更，此时请按状态提示检查 Gist。

GitHub 的秘密 Gist 不会出现在公开列表中，但不提供严格的访问控制；任何拿到链接的人都可能读取其中的节点配置。不要把 Gist 链接、Token 或生成的 YAML 发给不信任的人。

设置文件会明文保存 Token，也会保存配置/订阅/节点输入、输出路径、筛选条件、测速地址和高级参数：

```text
%APPDATA%\ClashSpeedTestGUI\settings.json
```

Token 输入框默认用星号遮挡；点击眼睛按钮可切换显示。不要分享该设置文件；清理或转交电脑前，应在 GUI 中清除保存的输入和凭据，或删除整个设置文件。

## 高级设置

点击“展开高级设置”可以修改节点正则过滤、测速模式、排除关键词、测速地址、下载量、探测超时、下载超时、最大 HTTP 探测失败率、下载速度下限和 User-Agent。节点正则过滤在测速前按节点名称生效，留空表示测试全部节点。旧设置中的上传字段会在读取时忽略、下次保存时清除；旧 `SpeedMode=full` 自动迁移为 `download`，原 `TimeoutSeconds` 继续表示下载超时。

高级设置里有两个不同的并发参数：

- **延迟并发**：同时对多少个节点做低流量 HTTP 探测；快速方案默认 `8`，均衡和深度方案默认 `4`。下载阶段始终逐节点执行。
- **单节点下载连接**：当前节点进入下载阶段时同时建立多少条连接；仅在 `download` 模式生效，范围为 `1–16`，`fast` 模式会自动禁用。

原版命令行程序没有被替换，仍可直接运行：

```powershell
.\clash-speedtest.exe -h
```
