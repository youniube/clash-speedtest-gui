# MEMORY.md

## 项目概况

- 项目名：clash-speedtest_Windows_x86_64
- 初始化日期：2026-07-03
- 类型：Windows x86_64 预编译命令行工具
- 主要程序：`clash-speedtest.exe`
- 项目来源线索：README 指向 `github.com/faceair/clash-speedtest`
- 技术栈：WinForms/.NET Framework GUI、Go 订阅解析器与并发测速执行器；源码位于 `src/` 和 `tools/`

## 已确认的使用特征

- `clash-speedtest` 是命令行工具，不是图形化界面程序。
- 运行时需要通过 `-c` 指定本地 Clash/Mihomo 配置文件或 HTTP(S) 订阅地址。
- 直接双击会打开临时控制台；程序输出提示或错误后退出，窗口随即关闭，看起来像“闪退”。

## 环境与约束

- 工作目录：`F:\codex\clash-speedtest_Windows_x86_64`
- Shell：PowerShell
- `~/.codex/templates/` 在初始化时不存在，因此两个项目约束文件采用最小可用结构创建。

## 决策与踩坑

- README 当前在 PowerShell 默认读取时显示乱码，属于文本编码显示问题，不影响可执行文件运行。
- 图形界面采用独立 WinForms 包装程序 `Clash-SpeedTest-GUI.exe`，通过重定向 TSV 输出调用本地 `speedtest-runner.exe`；执行器基于上游 1.8.8 Go 包做本地适配，原命令行程序保持不变。
- GUI 设置保存在 `%APPDATA%\ClashSpeedTestGUI\settings.json`；按用户选择，Gist Token 明文保存但界面默认遮挡，凭据值不写入项目文档。
- 重命名由内核取得国家码后生成受控占位符，再由 GUI 转换成 `🇭🇰 香港 HK-01` 格式；地区识别失败时保留原名。
- 构建入口是 `build-gui.ps1`，使用 Windows 自带的 .NET Framework C# 编译器生成无控制台窗口的 x64 EXE。
- Windows PowerShell 5.1 会把无 BOM 的 UTF-8 脚本按本地编码读取，因此构建脚本保持纯 ASCII，并为 C# 编译显式设置 UTF-8 codepage。
- 订阅 URL 需按查询字符串规则添加 `flag=meta`：无 `?` 时使用 `?flag=meta`，已有查询参数时使用 `&flag=meta`；GUI 会自动规范化。
- 上游内核在订阅返回非 Clash YAML 但可被 YAML 解码为空结构时可能以退出码 0 结束；GUI 必须以 TSV 节点行数额外判断是否真正加载到节点。
- GUI 先让内核写入同目录临时文件，确认加载到节点后才覆盖目标文件，避免无效订阅或中止测试损坏已有输出。
- 多格式订阅解析由本地 `subscription-parser.exe` 负责，源码在 `tools/subscription-parser/`；它复用 Mihomo `common/convert`，不经过在线订阅转换服务。
- 解析顺序为 Clash/Mihomo YAML 优先，失败后回退原 URL 并识别 Base64 或明文协议链接；当前覆盖 VLESS、VMess、Trojan、SS/SSR、Hysteria/Hysteria2、TUIC、AnyTLS、HTTP 和 SOCKS。
- GUI 把解析后的临时 YAML 交给并发测速执行器，并在结束、失败或取消后清理 `%TEMP%\ClashSpeedTestGUI\` 下对应的单次任务目录。
- 上游 `-concurrent` 实际表示单节点下载连接数，且 `TestProxies` 串行遍历节点；GUI 改用本地 `speedtest-runner.exe` 提供独立的节点级工作池并发，原 `clash-speedtest.exe` 仍保留。
- 并发执行器会先输出完整节点清单，GUI 一次性建立所有“等待”行，随后按节点名称原位更新结果，不再测完一个才新增一行。
- 有效节点是导出的硬性条件：延迟必须大于 0、丢包必须低于 100%；download/full 还要求对应传输实际成功，再应用用户设置的阈值。
- 2026-07-03 使用用户指定订阅做 fast 实测：42 个去重节点下，节点并发 1 用时 154.9 秒，并发 4 用时 39.8 秒，约 3.9 倍加速；凭据本身未记录。
- 2026-07-03 全项目审查确认：GUI 实际测速链路是 `Clash-SpeedTest-GUI.exe -> subscription-parser.exe -> speedtest-runner.exe`，GUI 不调用 `clash-speedtest.exe`；当前对原内核的存在检查属于冗余发布依赖。
- `cs.yaml` 含多行节点凭据字段，属于本地敏感产物；凭据值不记录，后续应移出源码/发布目录并加入忽略规则。
- Go 1.26 下两个本地模块曾因 module path 不含域名导致 `go test -cover` 报 `cannot find package`；现已改为带域名的本地模块路径并恢复覆盖率测试。
- 用户明确项目只供本人本地使用，凭据明文存储暂不处理；当前优先级是修复功能和可靠性缺陷。
- 输出文件和设置文件采用同目录临时文件后原子替换，失败时保留旧文件，避免覆盖过程中产生半截文件。
- Go 1.26 在当前非 Git 发布目录构建时需要禁用 VCS stamping；构建脚本已固定使用 `-buildvcs=false`。
- 2026-07-03 GUI 已移除对未参与实际调用链的 `clash-speedtest.exe` 强制检查；日常图形版只要求 GUI、订阅解析器和并发测速器三个 EXE，原命令行程序作为可选工具保留。
- 零节点通过筛选时不再提交临时输出，已有结果保持不变；只有至少一个有效节点时才替换输出并继续 Gist 更新。
- GUI 提供快速、均衡、深度三个测速方案；均衡方案默认每节点最多约 20MB，并明确显示按节点计算的流量影响，手动调整参数后标记为自定义。
- GUI 增加“打开结果位置”和“复制结果路径”，主 README 改为图形版快速入口，上游命令行说明降为次要参考。
- 2026-07-04 上游 v1.8.8 按 `server:port` 去重会把 71 个订阅节点错误合并为 42 个；当前测速层已引入到 `tools/speedtest-runner/internal/upstream/` 并移除该规则，统一采用完整配置指纹去重：仅排除顶层 `name`，其余协议、地址、端口、凭据、传输和 TLS 等字段经稳定 YAML 序列化后计算 SHA-256，完全一致才合并，指纹失败则保留节点。实测该订阅最终保留 68 个节点，即存在 3 个仅名称不同的真实重复项。
- 当前“延迟”是经节点向测速 URL 连续发起 6 次 HTTP HEAD 后取成功样本平均值，抖动是成功样本标准差；“丢包率”实际是 HTTP 请求失败率，并非 ICMP/UDP 网络层丢包。下载测速使用 Range GET 和多连接汇总；节点并发大于 1 时会争用本机总带宽，因此追求绝对速度应把节点并发设为 1。
- 2026-07-04 GUI 的 Gist 上传改为 Sub-Store 式自动管理：用户只填 GitHub 用户名和 Token；程序校验 Token 账号，按固定描述 `Clash-SpeedTest GUI` 查找专用 Gist，不存在时创建私有 Gist，存在时更新同名输出文件，不再要求用户提供 Gist ID。
- 2026-07-04 GUI 输入框支持单个订阅地址、本地文件、单节点 URI 和每行一个节点的多行批量输入；连续节点行会先写入单次任务临时文件并合并解析，避免大批节点逐个启动解析进程。HTTP(S) 仍按远程订阅读取，避免把订阅地址误判为 HTTP 代理节点。
- 2026-07-04 解析器与测速器已同步升级到 Mihomo `v1.19.27`；测速层基于 `clash-speedtest v1.8.8` 本地适配新版 `provider.ParseProxyProvider` 接口，并禁用错误的 `server:port` 去重。单元测试、完整构建和 `cs.yaml` 回归均通过：升级前后解析与加载均为 37 个节点，实际快速测速返回 37 行，其中 33 个节点可达。
- 2026-07-05 部分订阅服务器或本机网络代理可能在 HTTP 响应前瞬时断开并返回 `EOF`；订阅解析器现会在原 30 秒总超时内对网络错误、响应读取错误、HTTP 429 和 5xx 最多尝试 3 次，并将默认 User-Agent 同步到 Mihomo `v1.19.27`。
- 用户希望后续支持“电脑作为测速后端、手机作为网页前端”，并要求手机在外出、不处于电脑同一局域网时也能安全访问；远程入口不得直接通过路由器端口映射暴露。
- 手机和电脑都需要同时运行各自的 VPN/代理客户端，因此远程控制不能依赖 Tailscale、ZeroTier 等占用系统 VPN 通道的组网方案；优先采用不要求手机安装 VPN 客户端的 HTTPS 出站隧道，并保留身份认证。
- 远程手机前端确定采用浏览器网页方案：Windows 本地控制服务仅监听回环地址，由 Cloudflare Tunnel 发布 HTTPS 入口，并使用 Cloudflare Access 做登录认证；实施顺序是先完成本地 Web API/移动端页面，再接入隧道，最后验证电脑和手机保持 VPN 时的完整链路。
- 2026-07-05 GUI 顶部输入提示改为独立行并为 DPI 缩放预留垂直间距，节点筛选在界面与设置中默认留空、运行时仍等价转换为全匹配；最终状态栏固定保留总数、有效数、失败数，并单独显示 Gist 创建、更新、未启用或失败结果。
- 2026-07-10 按用户要求暂不处理 Git 仓库卫生和根目录敏感 YAML 迁移；本次只补可靠性项：测速器远程配置读取增加 HTTP 状态码检查、超时和 32MB 大小限制，provider/url/server 字段改为类型安全校验，GUI Gist 查找改为分页，新增 `test-all.ps1` 串联 GUI 自测和两个 Go 模块测试。
- 2026-07-10 功能优化需求评审确认：GUI 当前没有独立 Node 模型，列表只是 `DataGridView` 字符串行；完整节点配置仅保留在 Go 测速器的 `CProxy.Config` / `Result.ProxyConfig` 中，GUI 与测速器之间只传清单和 TSV 指标。后续复制 URL、Clash Meta、排序与管理功能应先以小步方式补充稳定节点 ID 和只读节点 DTO，不改测速算法。
- 2026-07-10 第一轮节点列表优化采用兼容事件协议：测速器继续输出原 TSV，同时通过 Base64 JSON 事件向 GUI 传递完整配置、稳定配置指纹 ID、分享 URL 和结果关联 ID；GUI 行以只读 `NodeSnapshot` 绑定，不再依赖节点名称关联。Clash Meta 直接由内部配置生成，URL 由本地协议序列化器生成；不能安全生成时整次复制失败并保留原剪贴板。编辑和删除入口留到第二阶段。
- 2026-07-10 第一轮真实订阅回归使用公开地址 `https://raw.githubusercontent.com/Alex0510/onskr/refs/heads/main/output/nodes.txt`：解析得到 200 个 Trojan 节点，fast 模式、1 秒超时、节点并发 32 用时约 25 秒，171 个节点可达；200 个节点事件、结果事件、稳定 ID 和分享 URL 全部完整且无生成错误。200 条分享 URL 回灌解析后核心与可选字段均无差异，200 条 Clash Meta 配置回灌后逐项完全一致；临时凭据产物在验证后删除。
- 2026-07-10 第二轮节点管理确定只允许重命名和从本地筛选结果删除，不开放服务器、端口或凭据编辑，因为连接参数变化会使现有测速指标失效。操作以稳定配置指纹定位节点，由测速器的本地配置管理模式读取并重写 YAML，GUI 使用同目录临时文件原子替换；原订阅始终不回写，启用 Gist 时在本地保存成功后再同步。重命名会刷新完整配置与分享 URL，删除支持多选和空结果；单元测试、GUI 自测、完整构建及两节点端到端管理样例均通过。
- 2026-07-10 第二轮变更审计：当前目录不是 Git 仓库，无法提交级 diff；通过文件检索、`main.go` 备份对比和 `test-all.ps1` 确认第二轮主要增加本地输出 YAML 的重命名/删除管理模式，完整回归通过。
- 2026-07-10 用户纠正：不能把当前第二轮状态记为“已完成”。实际只完成了本地结果重命名和删除，仍缺少备注编辑以及四类筛选功能；后续推进第二轮时应从这些缺口继续。
- 2026-07-10 第二轮现已补齐并通过验收：节点编辑同时支持名称和备注，备注按稳定配置 ID 保存在本机 GUI 设置中，不写入 Clash YAML、原订阅或 Gist；未导出节点只允许编辑备注。列表新增状态、延迟、协议、地区四类组合筛选，条件按“同时满足”生效且只改变列表可见性，不改写输出文件。
- 2026-07-10 第二轮最终验证：三个 EXE 重新构建成功，GUI 自测、订阅解析器及测速器全部 Go 测试通过；公开真实订阅仍解析为 200 个节点，抽取 1 个节点联网测速成功返回协议 v2、节点和结果事件，随后在完整 200 节点配置上实测重命名 1 个、删除 1 个，稳定 ID 保持不变，199 节点管理结果可被解析器完整回灌；验证临时目录已清理。
- 2026-07-11 第三轮锁定范围：主列表统一为“序号、节点名称、类型、延迟、下载速度、上传速度、出口地区、状态”八列，三种模式用“未测试”补齐未执行指标；动态序号按当前可见顺序重排，节点操作继续使用稳定 ID；状态栏固定统计总数、筛选后、已选、有效、失败、等待。彻底移除备注、详情区、出口 IP、ASN 和运营商，旧设置中的 `NodeNotes` 启动时原子清除，编辑入口恢复为仅重命名。
- 2026-07-11 出口地区查询决策：只对本轮已导出的有效节点手动查询，结果不写 YAML、不跨测速持久化且重新测速清空；每个请求必须走对应节点代理。查询器采用独立 `RegionProvider` 与顺序回退链，固定使用 IPWHOIS.io、FreeIPAPI、IP.SB，单服务超时 4 秒、单节点总预算 12 秒、节点并发 8；429/403 在本批次暂停提供商，连续 3 次网络错误或 5xx 后熔断，取消时不再回退。事件只含稳定 ID、地区、提供商与错误，不记录出口 IP；地区筛选只匹配已查询的 ISO 国家代码。
- 2026-07-11 第三轮实施与验收：GUI、订阅解析器和测速器三个 EXE 已重新构建，`test-all.ps1` 全部通过；测速器的提供商测试覆盖成功、城市缺失、403/429、5xx、超时、畸形 JSON、业务失败、顺序回退、批次暂停、连续失败熔断、取消和节点并发隔离。公开 200 节点订阅仍解析为 200 个节点，快速模式产生 200 个流式结果事件并导出 175 个有效节点；抽取其中 2 个节点真实查询时均通过各自代理由 IPWHOIS.io 返回中文“🇸🇬 新加坡”，全部验收临时文件已清理。Windows 界面捕获组件因 `SetIsBorderRequired` 接口不受支持未能生成截图，但 GUI 自测已覆盖固定八列、占位值、排序、动态序号、状态统计、地区格式化/筛选和旧备注迁移。
- 2026-07-11 用户认为原 GUI 标志不美观；现改为蓝色圆角底、白色闪电与三节点网络结构的高对比图标，优先保证 16×16/32×32 下的辨识度。透明 PNG 与多尺寸 ICO 位于 `assets/`，构建脚本通过 `/win32icon` 嵌入 EXE，窗口运行时也显式读取关联图标；重建和 `test-all.ps1` 均通过，实际从 EXE 提取的 32×32 图标已验证。
- 2026-07-11 用户用附件中的深蓝圆角底、蓝青渐变发光 S 形图替换上一版 GUI 标志；原图保持 1254×1254，去除 JPEG 黑色角落后保存为透明 PNG，并生成含 16/24/32/48/64/128/256 像素帧的 ICO。GUI 已重建，完整测试通过，且从最终 EXE 提取的 32×32 图标已确认清晰可辨。
- 2026-07-11 第一轮核心可靠性修复完成：深度模式遇到带路径、查询或片段的直接下载地址时由 GUI 预检并由测速器硬性拒绝，不再静默降级；延迟请求超时与延迟筛选阈值分离；多连接速度按整批墙钟耗时计算；下载响应严格限制目标字节数，206 校验 Content-Range，短响应失败。
- 2026-07-11 多输入源同名节点不再按名称静默丢弃：按输入顺序和源内名称排序，第一个保留原名，后续不同配置使用 `原名 [2]`、`原名 [3]`，并避让真实占用名称；自动编号同步到事件和导出配置，但完整配置稳定 ID 继续排除 name，因而保持不变。GUI 同时拒绝输出覆盖任何本地输入或 GUI、解析器、测速器程序文件。
- 2026-07-11 本轮新增深度模式、延迟超时、批次吞吐、Range/短响应、同名多源和路径冲突回归测试；两个 Go 模块无缓存测试与 go vet、GUI 自测、三 EXE 重建及完整 test-all 均通过。按用户决定继续暂缓迁移根目录真实 `cs.yaml`、`wa.yaml`。
- 2026-07-11 下一步从第二轮可靠性修复开始，第一项处理任务状态与取消：测速或地区任务运行期间锁定会改变实际行为的输入、输出、模式和 Gist 控件，引入贯穿解析、测速、结果后处理与 Gist HTTP 请求的统一取消机制，保证点击“停止”后不再继续写输出或上传；完成后再处理事件协议严格校验和大节点列表批量刷新性能。
- 2026-07-11 第二轮可靠性修复完成：测速与地区查询统一使用单任务取消上下文，任务参数在运行期间锁定；停止可终止解析器、测速器、地区查询、提交后的节点清单读取和等待中的 Gist 请求，关闭窗口会等待任务清理后再退出。取消发生在原子提交前时旧输出不变；提交后取消会保留已保存的本地结果，并提示 Gist 最终请求可能已经被 GitHub 接收、无法由客户端回滚。
- 2026-07-11 事件协议与大列表收口：GUI 严格校验 v2 版本、固定表头、节点总数、稳定 ID、完整节点配置和一一对应的结果；Go 端所有协议写入与 Flush 错误均向上返回，兼容 TSV 会清洗控制字符且不参与业务判定。最终有效数以测速器实际生成的临时 YAML 为权威来源，空 `proxies: []` 不会覆盖旧文件。列表采用任务 ID 隔离的 FIFO 队列，每 50ms 最多处理 200 条，最终也分批让出 UI 线程后排空。
- 2026-07-11 第二轮最终验收：三个 EXE 已按最终源码重建，GUI 自测、两个 Go 模块无缓存测试、`go vet` 和 `test-all.ps1` 全部通过；本地不可达节点协议集成样例产生完整 8 行 v2 transcript 且导出 0 个节点，临时样例已删除。`cs.yaml`、`wa.yaml`、`filtered.yaml` 的 SHA-256 与本轮开始时一致。
- 下一步优先考虑协议 v3：由测速器在结果事件中直接携带原始指标和权威 `usable` 状态，减少 GUI 对格式化文本的瞬时推断；同时为运行中的 Gist HTTP 取消和子进程启动/阻塞取消补可注入的集成测试。
- 2026-07-11 测速事件协议已升级为严格 v3：结果事件固定携带 `usable`、纳秒级延迟/抖动、百分比丢包率、bytes/s 传输速度及实际传输执行标志；`cells` 仅负责显示。GUI 会核对精确键集合、JSON 原始类型、数值范围、模式约束和稳定 ID，并直接用原始值完成状态、排序与筛选；最终临时 YAML 仍按 ID 逐项复核，事件与导出不一致时拒绝提交。
- 2026-07-11 取消机制新增可执行集成自测：Gist 使用实例级 `HttpMessageHandler` 注入，验证第二个请求运行中取消、无后续 POST/PATCH 及非调用方取消的超时分类；子进程使用同一 GUI EXE 的隐藏阻塞模式，覆盖启动前取消、`ReadToEndAsync`、异步行事件、同步 `ReadLine` 三种阻塞解除。生产进程统一由 lease 管理，未确认退出时释放会主动终止，避免读取或解析异常遗留后台进程。
- 2026-07-11 v3 最终验收：`speedtest-runner` 版本升至 1.4.0，三个 EXE 同步重建；GUI 自测、两个 Go 模块无缓存测试、`go vet` 与 `test-all.ps1` 全部通过。本地不可达节点的正式 EXE 集成测试产生完整 8 行 `@protocol 3` transcript，固定 4 个顶层字段和 7 个原始指标字段齐全，`usable=false` 且最终导出 0 个节点；测试临时文件已删除。
- 2026-07-11 出口地区事件流已严格化为独立 v2 状态机：顺序固定为协议版本、声明数量和一一对应的稳定 ID 结果；C# 与 Go 同时拒绝错误版本、乱序、重复、未知、缺失、畸形 Base64/UTF-8/JSON、错误原始类型及成功/失败字段冲突。Go 请求 JSON 同时拒绝未知字段、尾随值、空白、重复和未规范化 ID，写入与 Flush 错误均向上传递。
- 2026-07-11 地区查询改为整批事务：后台只暂存已验证事件并报告进度，只有退出码 0 且完整性通过后才在 UI 线程整批提交；取消、协议错误或非零退出会恢复刷新前的 State/Country/City/Emoji/Error 完整快照。地区筛选只读取成功状态，筛选选项仅在提交后重建，回滚与提交都会恢复当前用户排序；单节点查询失败原因显示在地区单元格悬停提示中。
- 2026-07-11 地区查询取消链采用有界结果通道，feeder、worker、提供商调用和结果发送均监听 context；写入失败会取消整条生产链，不再继续调度剩余节点。GUI 自测新增地区专属隐藏子进程，覆盖畸形事件后阻塞清理、退出码 0 但缺失事件、非零部分流和阻塞读取时取消。
- 2026-07-11 地区协议最终验收：`speedtest-runner` 升至 1.5.0，三个 EXE 已重建；GUI 自测、两个 Go 模块无缓存测试、`go vet` 和 `test-all.ps1` 全部通过。正式 EXE 对空配置中的未知 ID 输出完整三行 v2 失败事件，对重复请求 ID 在输出表头前退出 1；集成临时文件已删除，`cs.yaml`、`wa.yaml`、`filtered.yaml` 哈希保持不变。
- 下一步建议进行真实 WinForms 操作级自动化：覆盖按钮启停、刷新已有地区、取消/失败回滚、筛选与排序联动、高 DPI 布局和窗口关闭清理；自动化稳定后再评估手机 Web 前端。
- 2026-07-12 真实 WinForms 操作级回归采用 `tests/ui/prepare-ui-fixture.ps1` 创建独立 `%TEMP%` 沙箱，并由 `UiFixtureLauncher.exe` 隔离设置目录、TEMP/TMP 和相邻进程；固定夹具覆盖测速成功/阻塞、地区整批成功/取消/畸形/缺失/非零退出且不访问外网。Windows 截图组件因 `SetIsBorderRequired` 接口不受支持仍无法截图，本轮通过 UI Automation 可访问性树、稳定控件 ID、键盘输入和沙箱信号完成真实操作验证。
- 2026-07-12 GUI 操作可靠性收口：节点管理纳入统一任务取消与关闭清理；序号列禁止排序；输出路径变化即时刷新地区查询按钮；删除后重建地区选项并重套筛选；关键控件增加稳定 `Name` 和显式 TabIndex。窗体使用 `ProcessCmdKey` 统一处理 `F5` 开始、`F6` 查询和 `Esc` 停止，避免多行文本框或结果表格吞掉快捷键；组合框展开时 Esc 不会误停任务。
- 2026-07-12 DPI/布局与发布收口：GUI 加入 PerMonitorV2 manifest、运行时 DPI 自检、可缩放且保持结果表最小高度的选项区布局，以及窗口工作区约束；构建脚本嵌入 manifest。`test-all.ps1` 固化为 GUI 自测、两个 Go 模块 `-count=1` 无缓存测试和 `go vet`。
- 2026-07-12 最终敏感文件复核发现 `wa.yaml` 的修改时间为 11:43，早于本轮首次构建，且哈希已不同于 2026-07-11 记录；本轮沙箱、构建和测试均未读取或写入该文件，因此未擅自还原，后续由用户确认该外部/此前变化是否保留。
- 2026-07-12 真实 GUI 最终验收通过：受控测速显示 3 个节点、2 有效/1 失败并原子生成两节点 YAML；测速运行态锁定配置且停止可恢复；地区查询在 1/2 时取消会完整回滚，畸形协议会弹错并回滚，完整批次提交 2/2 后按钮状态正确，三种场景均不改写输出 YAML。关闭运行中任务会确认、终止子进程并清理临时目录。三个 EXE 已重建，升级后的 `test-all.ps1`（GUI 自测、两个 Go 模块无缓存测试和 `go vet`）全部通过，UI 沙箱、临时日志和残留进程均已清理。
- 2026-07-12 用户偏好：需要 Windows 界面截图时优先调用本机 PixPin，使用 `Ctrl+1` 触发截图并读取保存的 PNG 做视觉验证；用它绕过当前 Windows 10 上 `SetIsBorderRequired` 接口不受支持的问题。
- 2026-07-12 用户取消手机 Web 前端与 Cloudflare Tunnel 方向；本轮未创建手机端代码、账号、域名或外部配置，后续不再把该方向作为待办。
- 2026-07-12 Git 与敏感配置收口：空的无效 `.git` 已替换为本地有效仓库，根目录真实 `cs.yaml`、`wa.yaml` 在保持内容不变的前提下迁移到 `%LOCALAPPDATA%\ClashSpeedTestGUI\private-inputs\`；`wa.yaml` 的此前修改按保留现状处理。`.gitignore` 排除真实 YAML、默认输出、EXE 和测试产物，避免凭据或构建产物进入版本历史。
- 2026-07-12 UI 节点管理夹具已实现 `-manage-config`，支持按稳定 ID 重命名/删除并重写沙箱 YAML；`test-all.ps1` 新增“测速导出、重命名、删除、重新读取”契约回归且完整测试通过。Windows 捕获接口仍返回 `SetIsBorderRequired 0x80004002`，因此没有把鼠标点击/弹窗视觉层伪报为自动化通过。
- 2026-07-12 GitHub 调研确认 `SetIsBorderRequired 0x80004002` 来自 Codex Computer Use 在 Windows 10 build 19045 上把较新的可选边框属性当成必需步骤，并非本项目 GUI 缺陷；OpenAI Codex 相关问题仍处于开放状态。替代优先级确定为：本项目操作回归采用 C# FlaUI/UI Automation，截图使用 `PrintWindow(PW_RENDERFULLCONTENT)` 后回退屏幕区域 `BitBlt`，视觉复核继续允许 PixPin；不修改会随插件更新被覆盖的闭源助手二进制。完整开源 CUA 参考位置为 `github.com/trycua/cua`，其 Windows 实现会忽略边框设置失败并提供 WGC、PrintWindow、BitBlt 回退链，但对本项目而言引入整套平台过重。
- 2026-07-13 Windows 10 操作级回归已落地为项目自有 C# FlaUI 驱动：固定使用 FlaUI 5.0.0 的 .NET Framework 程序集和 SHA-256 校验缓存，不要求安装 .NET SDK；真实完成三节点测速、重命名 A、删除 B、YAML 复核及最终 PNG 捕获。默认 `test-all.ps1` 仍无桌面干扰，只有显式传入 `-IncludeWinFormsUI` 才打开隔离测试 GUI；测试不调用或修改 PixPin。
- 2026-07-13 Windows 10 上 FlaUI 高层 `DataGridView` 行枚举可能阻塞，当前稳定策略是用 Automation ID 找到结果表，再按表格边界、DPI 和 WinForms 菜单尺寸操作行与右键菜单；原生完成/确认弹窗单独发现并关闭。截图优先 `PrintWindow(PW_RENDERFULLCONTENT)`，无效时回退屏幕区域 `BitBlt`。
- 2026-07-13 发布决策：项目远程位置为私有 GitHub 仓库 `github.com/youniube/clash-speedtest-gui`；首个正式版本为 `v1.0.0`，Windows x86_64 发布包只包含 GUI、订阅解析器、测速器三个 EXE 及 README、GUI 使用说明和 GPL-3.0 许可证，不发布上游 `clash-speedtest.exe`、本地配置或开发记忆文件。
- 2026-07-13 全项目只读复核发现正式发布前仍有阻断项：无路径测速服务只设置 `serverBaseURL`，延迟探测却固定使用空的 `downloadURL`，导致 `download/full` 的服务端模式在代理拨号前即被判为 100% 请求失败，其中 GUI 深度模式必然不可用；本机 mock 已复现。现有完整回归与 WinForms happy path 均通过，但未覆盖真实测速算法的该分支。另确认首次默认值为 `5MB/s + 自定义` 而非文档所述均衡 `3MB/s`、订阅响应 32MiB 限制会静默截断、合法逗号 URL/路径会被拆源、协议筛选项不完整；发布前应先修核心分支并补端到端测速回归，再处理输入完整性和文档/版本可追溯性。
- 2026-07-13 测速调度正式改为“两阶段”：节点间只并发执行 1 次预热加 5 次正式低流量 HTTP 探测，通过初筛后逐节点执行吞吐，同一节点内部保留多连接；延迟采用正式成功样本中位数，“丢包率”统一澄清为 HTTP 探测失败率。传输结果协议升级为 v4，以 `tested` 和 `complete` 区分未启动、部分采样与完整传输；部分速度可以展示，但未完整传完的节点不得导出。
- 2026-07-13 发布修复目标合并为 `v1.1.0`：已解决无路径测速服务、订阅 32MiB 静默截断、逗号输入、首次默认值、动态协议筛选、结构化重命名和 provider 双拉取/错配；`test-all.ps1` 默认从当前源码重建后再回归。最终无缓存 Go 测试、`go vet`、严格协议夹具和真实 WinForms 操作回归均通过，操作截图由 `PrintWindow(PW_RENDERFULLCONTENT)` 成功捕获。
- 2026-07-13 `v1.1.0` 修复已推送到私有仓库分支 `agent/v1.1.0-speedtest-reliability`，草稿 PR 位于 `github.com/youniube/clash-speedtest-gui/pull/1`；合并到 `main` 后再创建正式标签与 Windows x86_64 Release，避免标签指向未审阅分支。
- 2026-07-13 PR #1 已 squash 合并到 `main` 提交 `009a5ca`，正式 `v1.1.0` Release 位于 `github.com/youniube/clash-speedtest-gui/releases/tag/v1.1.0`；发布包从该提交重建并通过完整 WinForms/Go 回归，远端下载后 SHA-256 复核为 `b945066062bdb4916a929f2c20abc0b8ae9eb3cd48d52a6ff1f72159e7dbac79`。
- 2026-07-13 本地源码的测速协议升级为严格 v5，取代此前 v4 方案：每个稳定节点 ID 必须先且只能出现一次 `probe_completed`，下载候选随后出现一次 `download_started`，最终结果按节点立即 Flush；GUI 因此能实时区分等待探测、等待下载、下载中和最终状态，快速模式不再等待整批探测。节点间下载保持串行，单节点内部保留多连接。
- 2026-07-13 测速功能统一为纯下载：GUI 固定七列并明确使用“HTTP 延迟”“HTTP 探测失败率”，执行器只接受 `fast`/`download`，探测超时与下载超时独立。深度方案改为 50MB、4 条下载连接和更严格阈值；旧 `full` 自动迁移为 `download`，旧上传字段读取后清除，旧 `TimeoutSeconds` 继续代表下载超时。下载速度只代表“当前电脑网络 → 代理节点 → 当前测速服务器”的本次路径吞吐。
- 2026-07-13 自动重命名不再读取入口 `server`：测速临时结果中的有效节点通过各自 Mihomo 代理执行现有地区查询，成功后按 `🇯🇵 日本 JP-01` 生成稳定 ID 重命名；单节点失败保留原名，整批协议异常不应用任何重命名，取消发生在本地原子提交前时保留旧输出，提交成功后才允许同步 Gist。地区事件仍不包含出口 IP、ASN 或运营商。
- 2026-07-13 本轮最终验收：从当前源码重建三个 EXE，GUI 自测、严格 UI 夹具、300 个模拟不可达节点、探测并发与下载串行、纯 GET/Range/部分传输算法、两个 Go 模块无缓存测试、`go vet` 和真实 FlaUI WinForms 操作回归全部通过；未提交、推送或发布版本。
- 2026-07-13 本轮发布版本确定为 `v2.0.0`，因为协议 v5、删除 `full`/上传 CLI 参数属于公开接口不兼容变更；GUI 程序集、manifest 与测速器版本统一为 2.0.0。Windows x86_64 发布包继续只包含三个运行 EXE、README、GUI 使用说明和 GPL-3.0 许可证，并附独立 SHA-256 文件。
- 2026-07-15 用户明确 Gist 采用 Sub-Store 式“单容器、多文件链接”语义：专用 Gist ID 只作容器，GUI 输出文件名作为 Gist 文件键；新文件名新增文件并产生新的稳定 `gist.githubusercontent.com/.../raw/文件名` 订阅链接，同名文件覆盖且链接不变，改文件名等同创建新链接。GUI 完成提示必须显示当前文件的 raw 链接，不再把固定 Gist 页面地址当作订阅链接。
- 2026-07-15 用户根据真实 GUI 上传结果进一步纠正 Gist 语义：不同 GUI 输出文件名不能复用同一个 Gist ID，必须分别创建独立秘密 Gist 并获得不同 raw 链接；只有同名远程配置再次同步时才查找并覆盖原 Gist、保持链接不变。上一条“单容器、多文件”决策被本条取代；旧容器不自动删除或迁移。
- 2026-07-15 列表筛选后的复制数量异常源于 WinForms 原生 `DataGridView.SelectAll()` 会重新选中隐藏行，而状态栏只统计可见选中行、列表操作却读取全部选中行。现统一为“当前可见且已选”语义：`Ctrl+A` 只选可见行，复制 URL/Clash Meta/名称、重命名、删除和选中节点地区查询都通过同一可见选择入口；筛选批量变更期间抑制中间统计刷新。GUI 自测覆盖隐藏行选择策略，真实 FlaUI 回归覆盖状态、延迟、协议、地区四类筛选后的全选与复制。
- 2026-07-15 地区筛选不再预置 US/JP/HK/SG/TW：测速完成但尚未查询真实出口时下拉框只显示“全部”并禁用；点击“查询有效节点出口地区”并整批成功后，只按本轮 `RegionState=成功` 的两位国家代码动态生成实际存在的选项，支持任意国家。节点名称中的 HK/US 等入口标签继续不作为出口地区依据；查询失败或取消沿用事务回滚，不产生空结果选项。GUI 自测与真实 FlaUI 已覆盖未查询、成功动态生成和失败回滚。
- 2026-07-15 用户明确节点分享 URL 的验收标准不仅是“能够生成和复制”，而是复制后的 URL 经生产订阅解析链路重新导入后必须能够实际建立代理连接；无法无损表达连接关键字段的协议配置必须继续拒绝复制，不得静默丢字段。
- 2026-07-15 GUI 将测速前的“节点筛选”从主界面移入高级设置并改称“节点正则过滤”，保留正则、设置兼容和运行逻辑；折叠状态下后续控件整体上移 34 像素，选项区由 455 缩至 421，使节点列表获得更多垂直空间。高级设置新增一行后保持原展开总高度；完整构建、GUI 自测、UI 夹具、两个 Go 模块测试及 `go vet` 全部通过，Windows 截图接口仍因 `SetIsBorderRequired` 不受支持而无法完成本轮目视截图验收。
- 2026-07-15 AnyTLS 分享 URL 已按 Mihomo URI 语义接入：无损保留 server/port/password/SNI/证书指纹/跳过校验/UDP，并对 ALPN、客户端指纹、ECH、客户端证书、会话调优、`udp=false` 等 URL 无法表达的非默认配置继续拒绝复制。测试覆盖特殊字符与 IPv6、生产转换器语义回环，以及“本地 AnyTLS 服务端 → 生成 URL → 重新导入 → Mihomo 建立代理 → HTTP 成功”的真实连接链路；完整构建、GUI 自测、UI 夹具、两模块测试及 `go vet` 通过。当前 `lin.yaml` 的 11 个 AnyTLS 均能生成并回环，但原配置和回环配置当时都无公网可达节点，不能作为公网连接成功样本。
- 2026-07-15 GitHub 同步前复核确认根目录 `lin.yaml` 是本地敏感输入，只用于 AnyTLS 回归，不属于源码或发布内容；已加入 `.gitignore`，后续提交和推送必须继续排除该文件。
- 2026-07-15 本轮正式发布版本确定为 `v2.1.0`：新增 AnyTLS 分享 URL 与真实连接回归、按输出文件隔离的稳定 Gist 链接、动态出口地区选项，并修复筛选后隐藏行仍参与批量操作的问题；继续兼容 `v2.0.0` 的测速协议 v5。
- 2026-07-15 PR #4 已 squash 合并到 `main` 提交 `2eac108`，正式 `v2.1.0` Release 位于 `github.com/youniube/clash-speedtest-gui/releases/tag/v2.1.0`；远端重新下载后的 Windows x86_64 ZIP SHA-256 已复核为 `be10fc11f3e202aff473d1169de2efcf27d3164f498dfff4e2e2ba562fcb5baf`。
- 2026-08-24 本轮可靠性修复采用失败封闭原则：任一配置源或 Proxy Provider 读取/下载/解析失败时整批失败，不再跳过后提交残缺结果；`type: file` Provider 支持 `proxies:` 与标准 `payload:`，本地相对 `path` 在订阅解析阶段固定为相对原 YAML 的绝对路径，确保 GUI 临时文件链路不改变含义。VLESS、Trojan、VMess、SS、SSR、Hysteria2、TUIC、AnyTLS 分享 URL 统一增加顶层字段、嵌套传输字段和冲突别名的无损校验；生产转换器无法恢复的 VLESS `skip-cert-verify`、VMess `client-fingerprint` 等配置明确拒绝复制。订阅 URL 的任意旧/重复/编码 `flag` 统一替换为唯一 `flag=meta`，并保持其他查询值的原始编码和 fragment；根目录本地敏感输入 `ss.txt`、`ss.yaml` 仅加入忽略规则，未读取或修改。三个 EXE 已从当前源码重建，GUI 自测、UI 夹具、两个 Go 模块无缓存测试、`go vet`、speedtest-runner race 检测和真实 FlaUI WinForms 操作回归全部通过。
- 2026-08-24 Provider 来源与依赖模型以 GUI 生成的版本化外部清单保存来源属性，不从落地后的临时 YAML 路径反推可信度；远程/inline/本地来源经过统一预备阶段，Provider 内容只获取一次后物化为 inline，并返回本轮实际读取的全部本地配置与 `type: file` Provider 依赖。远程来源一律禁止本机文件 Provider，GUI 在测速内核启动前和最终原子提交前都以实际依赖硬性检查输出冲突，冲突不进入普通覆盖提示。
- 2026-08-24 HTTP Provider 复用 Mihomo v1.19.27 的 HTTP 请求栈与 URI 转换器，支持认证 `header`、可检测超限的字节级 `size-limit` 和 URI 列表；命名 `proxy` 与 `age-secret-key` 因当前执行器没有可安全复用的隧道/解密上下文而在发请求前明确拒绝。踩坑：直接调用 `HTTPVehicle.Read` 会按限制静默截断且丢失现有错误正文摘要，因此改在 Mihomo HTTP 响应上执行 `limit+1` 检测并保留受控错误摘要。最终验证通过定向 Provider 分支、两个 Go 模块完整测试与 `go vet`、speedtest-runner 全量 `-race`、GUI 自测、严格 UI 夹具、真实 FlaUI 操作回归和 `%TEMP%` 独立三 EXE 构建；项目根目录 EXE 未替换。
- 2026-08-25 第一批安全最终收口继续采用“远程订阅不可信”模型：远程和 inline 来源的 HTTP Provider 每次建立连接都重新解析主机，并只向验证后的 IP 字面量拨号；IPv4-mapped IPv6 先还原为 IPv4，地址资格使用 2025-10-09 IANA IPv4/IPv6 Special-Purpose Address Registry 快照和最长前缀覆盖，`Globally Reachable=False`、空值或 N/A 失败封闭，更具体的 `True` 例外优先。未列入特殊用途注册表的 global-unicast 仍可尝试连接，所以该判断只表示通过已知特殊用途地址过滤，不保证实际公网路由；注册表更新时必须同步审计静态表。本地 YAML 来源继续允许用户明确配置本机或局域网 HTTP Provider。
- 2026-08-25 Provider 重定向安全策略在每一跳发送前执行并复用 Mihomo TLS/CA、inner tunnel 与 Dialer 语义：HTTPS 到 HTTP 一律拒绝；自定义 Header、URL userinfo 或任意查询参数存在时，仅允许 scheme、hostname、有效端口完全一致的严格同源重定向；无凭据 Provider 的跨源 CDN 跳转会先清除全部继承 Header 和 `Referer`，仅恢复程序默认 User-Agent，并继续使用受限拨号器。踩坑：默认重定向只会自动移除部分标准敏感 Header，任意自定义认证 Header和含查询凭据的自动 `Referer` 仍可能跨源泄漏，不能等最终响应返回后再检查 URL。
- 2026-08-25 最终收口验证先用修复前失败测试复现六个 IANA 非全局前缀被错误拨号、`2001::/23` 内更具体全局例外被误拦截、自定义 Provider Token 与查询参数 Referer 跨源泄漏及 HTTPS 降级继续发出第二次请求；修复后定向测试覆盖所有指定拒绝前缀（底层拨号 0 次）、公网 IPv4/IPv6/IPv4-mapped IPv6 和 IANA `True` 覆盖项、严格同源成功、无凭据跨源净化、降级请求前拒绝，以及原有私网重定向和 DNS rebinding 防护。`speedtest-runner` 全量无缓存测试、`go vet`、受影响包 `-race` 和 `git diff --check` 通过；本轮未构建或替换根目录三个 EXE。
- 2026-08-25 第二批节点合并不再用最终展示名称作为中间 map key：每个合法节点先按输入源顺序、排序后的 Provider 顺序和 Provider 内顺序进入携带来源、原始名称/配置及 filter/override 后配置的有序记录，全部过滤和 override 完成后再统一唯一命名并构建测速 map；自动编号预留所有真实名称，避免与用户已有 `[2]`、`[3]` 二次碰撞。修复前测试实际复现顶层 `[p] node` 被 Provider 覆盖、两个 override 同名节点只剩后者且 `LoadProxies` 仍成功，manifest/导出入口只能看到 3/4 个节点；修复后节点数、事件数、导出数和稳定 ID 一致。
- 2026-08-25 当前版本明确不支持非空节点字段 `dialer-proxy`，并与 Provider 下载字段 `proxy` 分开处理：顶层、inline/file/HTTP Provider 节点、Provider 级设置或 override 任一引入该字段，都会在任何节点探测、地区查询或输出提交前整批失败，错误只包含来源/Provider/节点定位，不回显完整配置或凭据。修复前实际拨号返回 `tunnel is invalid ... <nil>`，四类 Provider 均可继续物化，混合批次也会生成完整临时结果；完整支持必须另立兼容性项目同时处理 tunnel、缺失/循环/多级依赖、过滤闭包、唯一重命名引用和导出依赖。本批定向测试、runner 全量测试、`go vet` 与受影响包 `-race` 均通过，未构建或替换根目录 EXE。
- 2026-08-25 分享 URL 的“可安全复制”判定改为生成后立即调用生产 Mihomo `convert.ConvertsV2Ray` 回灌，并要求单条物理 URL、恰好一个节点及全部连接关键字段规范化语义等价；节点名称和导入器自动默认值单独处理。SS `obfs` 改用导入器识别的 `obfs`/`obfs-host` 参数；Trojan WS Host/early-data、SSR IPv6、SSR `udp-over-tcp` 等当前导入器不能恢复的配置明确返回脱敏错误。GUI 批量 URL 复制采用一次提交边界，任一节点无 URL 时不调用剪贴板提交且不生成半份结果；本批只处理 P2-2，根目录三个发布 EXE 未构建或替换。
- 2026-08-26 审查后修复按“输入完整性、主订阅下载安全、Provider 标准语义、Provider 同名、正则超时、统一版本构建、三进程端到端验收”顺序逐项推进。第一项已完成：顶层明文与 Base64 URI 订阅先逐个验证每个非空物理行必须恰好转换为一个节点，任一坏行以不含节点内容的行号错误使整源失败，再整批转换以保留 Mihomo 的同名编号语义；修复前回归已复现合法行被部分提交，修复后定向测试、解析器全量无缓存测试和 `go vet` 通过，根目录 EXE 留待统一版本构建阶段替换。
- 2026-08-26 用户要求暂跳过主订阅下载安全项并直接修复 Provider 原始同名节点。Provider 预处理现按原始记录索引而非名称标记已选项，使不同配置的同名记录都能各保留一次；已完成 filter/override 的记录以仅限 Mihomo 构造阶段的索引唯一名解析，绕开其原始名称去重，随后恢复项目统一展示命名与稳定 ID 语义。该路径同时取消了 filter/override 的第二次执行，但 `regexp2` 匹配超时仍归后续正则安全项；修复前回归复现 2 个同名节点只剩 1 个，修复后定向、runner 全量无缓存测试、`go vet` 和受影响包 `-race` 通过，根目录 EXE 未替换。
- 2026-08-26 Provider 正则安全项完成：`filter`、`exclude-filter` 和 `override.proxy-name` 全部经单一编译入口设置 250ms `regexp2` 匹配超时；库按约 100ms 周期检查，实际超时允许小幅上浮。任何匹配或替换错误都整批失败，`exclude-filter` 不再把超时当作未匹配，运行时错误会脱敏为规则类型与超时分类而不回显节点名；上一项已保证 filter/override 仅预处理一次。修复前定向测试确认默认超时为约 292 年，修复后覆盖三类灾难性回溯均约 0.4 秒失败且无部分结果，runner 全量无缓存测试、`go vet`、受影响包 `-race` 和差异格式检查通过，根目录 EXE 留待统一版本构建阶段替换。
- 2026-08-26 统一构建版本升为 `v2.2.0`：本轮属于向后兼容的可靠性与 Provider 能力增强，测速事件协议仍为 v5，因此不升主版本。GUI Assembly/文件/产品版本与 manifest 为 `2.2.0.0`/`2.2.0`，runner `-v` 自报 `2.2.0`；subscription-parser 沿用历史约定不新增公开版本参数，但三个 EXE 已由同一次 `build-gui.ps1` 从当前源码重建并覆盖根目录旧文件。最终 SHA-256：GUI `D076F582C73306B2CA55B24EDB5F5F759A8F26A8C94CE708D3BA24168A44DE4C`，parser `566C1B3054CC94F884E5F3A32337A0A39718005CCF943FBD7CCAEBF6AD6B9F30`，runner `D4CF083A26CA5ED684ED28A7F8BA1976523B8D8683DFEA5197DB43675E9884E7`。构建时间均晚于各自最新源码，`test-all.ps1 -SkipBuild` 的 GUI 自测、UI 夹具、两模块无缓存测试和 `go vet` 全部通过且测试前后哈希不变；三进程端到端失败封闭验收仍归下一项。
- 2026-08-26 第七项正式三进程验收已固化为 `tests/ui/test-production-e2e.ps1` 与 `ProductionE2eUiDriver.cs`，只在显式 `test-all.ps1 -IncludeProductionE2E` 时打开隔离 GUI：本地双 SOCKS5 探测端使正式 `GUI → subscription-parser → speedtest-runner` 成功保留并导出 Provider 内两个原始同名节点，混合 URI 坏行在 parser 阶段整批失败且不泄漏 URI 凭据，灾难性 Provider `filter` 在预备物化完成、确认覆盖后由测速 runner 于 250ms 匹配预算内失败，旧输出字节不变；实际阶段产物为 parser 配置 2、预备请求 2、物化配置 2、测速临时输出 1，整条正则 GUI 场景 1.638 秒。验收同时发现 `loadAndParse` 会吞掉已脱敏的 URI 行号错误并泛化为“不支持格式”，现改为把 `line 2` 诊断贯穿 CLI/GUI，并补 CLI 层回归。三个 v2.2.0 EXE 因此重新统一构建，最终 SHA-256 为 GUI `66A46871BC7699894722B58DEFEFA42E878195362F5FCFDCB5AA3AB34247F1FF`、parser `B93FA3A640C151EED4CF767C9173623E7B774473E10AE553E12E79D135B0AF5B`、runner `D4CF083A26CA5ED684ED28A7F8BA1976523B8D8683DFEA5197DB43675E9884E7`；生产 E2E、GUI 自测、UI 夹具、两 Go 模块无缓存测试和 `go vet` 全部通过，沙箱、临时输出和相关进程均清理，三个正式 EXE 测试前后哈希不变。
- 2026-08-26 `v2.2.0` 可靠性修复已推送到分支 `codex/v2.2.0-provider-reliability`，PR #5 位于 `github.com/youniube/clash-speedtest-gui/pull/5`。功能分支已合入远端 `main` 的 `v2.1.0` 发布记录；合并后从当前源码重新构建并通过 `test-all.ps1`、runner 全量 `-race` 和生产三进程 E2E。按用户要求跳过的主订阅下载安全项未包含在本 PR，已在 PR 说明中明确列为剩余范围。
